//用途：标记元素是否被访问（遍历）、表示开关状态、存储条件结果等。具有占用内存小、逻辑判断直观的特点。

//语法：bool 数组名[] = {};

/*
#include<iostream>
using namespace std;

int main()
{
	//在编程中，bool 数组（布尔数组）是由布尔类型（bool）元素组成的数组，
	//其每个元素仅能存储true（真）或false（假）两个值。
	bool Array[4] = {true,false,true,false};
//	bool Array[4] = {1,0,12,3};

	//内存占用：C++ 中，单个bool占 1 字节（8 位）
	cout << sizeof(Array) << endl;		//4

	for (int i = 0; i < 4; i++)
	{
		if (Array[i] == true)
		{
			cout << "Yes" << endl;
		}
		else
		{
			cout << endl;
		}
	}

	system("pause");

	return 0;
}
*/
